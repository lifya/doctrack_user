<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pln.www">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ikon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".TambahDokumentActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->

        <activity
            android:name=".DocumentTrackingActivity"
            android:label="Daftar Dokument"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MateriActivity"
            android:label="Materi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BerandaActivity"
            android:label="DocTrack PLN"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Detail_Proses_Activity"></activity>
    </application>

</manifest>